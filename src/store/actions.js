import * as types from './types'
export default {
    [types.VIEW_FOOT]:({state,commit},payload)=>{commit(types.VIEW_FOOT,payload)},
    [types.VIEW_LOADING]:({state,commit},payload)=>{commit(types.VIEW_LOADING,payload)},
    [types.CAR]:({state,commit},payload)=>{
        
    }
}